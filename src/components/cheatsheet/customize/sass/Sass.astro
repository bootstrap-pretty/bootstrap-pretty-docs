---
import HeadingSectionContent from "@components/cheatsheet/HeadingSectionContent.astro";
const { shiki, contents, slug } = Astro.props;
---

<HeadingSectionContent shiki={shiki} contents={contents} slug={slug} />
